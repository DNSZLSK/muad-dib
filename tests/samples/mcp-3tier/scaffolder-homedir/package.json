{
  "name": "scaffolder-homedir-fixture",
  "version": "1.0.0",
  "description": "Adjudication fixture: scaffolder writing an INERT template to the user-level .cursorrules — fires CRITICAL today via the user-level-path gate; the 3-tier classifier calls it `template` (candidate MEDIUM after the flip)"
}
